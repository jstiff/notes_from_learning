/// implementation of a DFA.
/// going to try and use the Rust DFA matching engine module for guidence.
/// regex::dfa
/// A DFA provides faster matching because the engine is in exactly one state at any point in time. In the NFA, there may be multiple active states, and considerable CPU cycles are spent shuffling them around. 


